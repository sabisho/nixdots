{
  programs.btop = {
    # Stylix: Remove these theme settings when using stylix.
    settings = {
      color_theme = "noctalia";
      theme_background = false;
      vim_keys = true;
    };
  };
}
